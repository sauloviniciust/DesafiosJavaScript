let primeiroNumero = prompt(
    'olá visitante! Digite o primeiro numero a ser somado:'
    );
    console.log(primeiroNumero);   
let segundoNumero = prompt(
    'olá visitante! Digite o segundo numero a ser somado:'
    );
    console.log(segundoNumero);

let soma = parseInt(primeiroNumero) + parseInt(segundoNumero);
alert(
    'Meu caro visitante, o resultado da soma é ' + soma
    );

console.log(soma);